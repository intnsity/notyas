# Generated by the esp-idf-sys build - never commit (sdkconfig.defaults is the source of truth).
/sdkconfig
# embuild workspace-local IDF/tools cache (we use ESP_IDF_TOOLS_INSTALL_DIR=global, but just in case).
/.embuild
/target
