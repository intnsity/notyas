// Extra bindgen surface for the Waveshare Touch-LCD-7 "X" (board-waveshare-7x):
// the waveshare/esp_lcd_ili9881c DSI panel driver. The DSI bus / LDO /
// panel-ops surfaces are already bound via bindings/st7703.h (bindings are
// a global union across all extra_components entries).
#include "esp_lcd_ili9881c.h"
